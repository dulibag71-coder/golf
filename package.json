{
    "name": "airswing-web",
    "private": true,
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build && npx -y shx mkdir -p dist/mobile-app && npx -y shx cp -r mobile-app/* dist/mobile-app/ && npx -y shx rm -rf dist/mobile-app/.git",
        "preview": "vite preview"
    },
    "dependencies": {
        "@mediapipe/camera_utils": "latest",
        "@mediapipe/drawing_utils": "latest",
        "@mediapipe/pose": "latest",
        "ammo.js": "^0.0.10",
        "bcryptjs": "^3.0.3",
        "cors": "^2.8.5",
        "dotenv": "^17.2.3",
        "express": "^5.2.1",
        "jsonwebtoken": "^9.0.3",
        "sqlite3": "^5.1.7",
        "three": "^0.160.0"
    },
    "devDependencies": {
        "vite": "^5.0.0"
    }
}